defmodule Slurp.LogsTest do
  use ExUnit.Case

  test ".load_subscriptions/1 can parse an explicit map for each filtered set of blockchains" do

  end

  test ".load_subscriptions/1 can parse a pipeline for each filtered set of blockchains" do

  end
end
